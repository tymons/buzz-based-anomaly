name: pytorch-cuda-env
channels:
  - defaults
  - conda-forge
  - comet_ml
  - pytorch
dependencies:
  - python==3.8.5
  - numpy
  - cudatoolkit=${CUDA_VERSION}
  - pytorch
  - scikit-learn
  - pyyaml
  - comet_ml
  - optuna
  - pip
  - pip:
    - torchsummary
